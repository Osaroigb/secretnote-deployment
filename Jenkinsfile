pipeline {
    agent any

    stages {
        // stage('Checkout') {
        //     steps {
        //         git branch: 'main', credentialsId: 'ssh_key', url: 'git@github.com:Osaroigb/privnote-deployment.git'
        //     }
        // }

        stage('Build') {
            steps {
                sh 'echo "Build completed successfully!"'
            }
        }

    }
}